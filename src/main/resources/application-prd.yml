spring:
  datasource:
    url: jdbc:postgresql://maglev.proxy.rlwy.net:23952/${PGDATABASE}
    username: ${PGUSER}
    password: ${PGPASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create
